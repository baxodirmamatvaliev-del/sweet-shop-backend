<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sweet Shop Admin - Users</title>
  <link rel="icon" href="/favicon.svg" type="image/svg+xml">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-page">
  <%- include('includes/header') %>

  <div class="floating-sweet fs4">🍪</div>
  <div class="floating-sweet fs2">🍰</div>
  <div class="floating-sweet fs3">🎂</div>

  <main class="container products-page">
    <div class="page-heading">
      <div>
        <p class="eyebrow">Admin panel</p>
        <h1>Users</h1>
        <p class="page-description">Review registered users and manage their account status.</p>
      </div>
      <a class="back-to-products" href="/admin/products"><span aria-hidden="true">←</span> Back to products</a>
    </div>

    <div class="table-card">
      <div class="table-scroll">
        <table class="products-table users-table">
          <thead>
            <tr>
              <th>User</th>
              <th>Phone</th>
              <th>Address</th>
              <th>Role</th>
              <th>Status</th>
              <th>Joined on</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (members.length === 0) { %>
              <tr>
                <td colspan="7" class="empty-table">No users found yet.</td>
              </tr>
            <% } %>
            <% members.forEach(function(member) { %>
              <tr>
                <td><%= member.memberNick %></td>
                <td><%= member.memberPhone %></td>
                <td><%= member.memberAddress || "—" %></td>
                <td><span class="role-badge"><%= member.memberType %></span></td>
                <td><span class="status-badge status-<%= member.memberStatus.toLowerCase() %>"><%= member.memberStatus %></span></td>
                <td><%= new Date(member.createdAt).toLocaleDateString('en-GB') %></td>
                <td>
                  <div class="status-dropdown">
                    <button type="button" class="btn-status-toggle" onclick="toggleDropdown('dropdown-<%= member._id %>')">Status ▾</button>
                    <div id="dropdown-<%= member._id %>" class="dropdown-menu">
                      <form action="/admin/users/<%= member._id %>/status" method="POST">
                        <input type="hidden" name="status" value="ACTIVE">
                        <button type="submit">Activate</button>
                      </form>
                      <form action="/admin/users/<%= member._id %>/status" method="POST">
                        <input type="hidden" name="status" value="BLOCK">
                        <button type="submit">Block</button>
                      </form>
                      <form action="/admin/users/<%= member._id %>/status" method="POST">
                        <input type="hidden" name="status" value="DELETE">
                        <button type="submit">Mark as deleted</button>
                      </form>
                    </div>
                  </div>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
  </main>

  <script>
    function toggleDropdown(id) {
      const menu = document.getElementById(id);
      const isOpen = menu.style.display === "block";
      document.querySelectorAll(".dropdown-menu").forEach(function(item) {
        item.style.display = "none";
      });
      menu.style.display = isOpen ? "none" : "block";
    }

    document.addEventListener("click", function(event) {
      if (!event.target.closest(".status-dropdown")) {
        document.querySelectorAll(".dropdown-menu").forEach(function(item) {
          item.style.display = "none";
        });
      }
    });
  </script>
</body>
</html>
